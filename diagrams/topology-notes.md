# Topology Notes

The Mermaid diagram in `topology.mmd` can be rendered directly in GitHub Markdown or converted to PNG using a Mermaid renderer.

Recommended future additions:

- Export an EVE-NG screenshot and place it as `diagrams/eve-ng-topology.png`
- Add interface labels
- Add IP addressing overlays
- Add traffic flow arrows for:
  - User to Internet
  - Branch to AD/DNS
  - DHCP Relay
  - Blocked inter-VLAN traffic
